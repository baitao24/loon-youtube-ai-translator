{
  "name": "loon-youtube-ai-translator",
  "version": "0.3.0",
  "private": true,
  "description": "DualSubs-based Loon YouTube bilingual subtitles with Gemini/OpenAI-compatible enhancement and official fallback",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/yt-ai-core.js && node --check src/yt-ai-loon.js",
    "test": "node --test test/*.test.js",
    "verify": "npm run build && npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
